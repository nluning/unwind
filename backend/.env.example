DB_HOST=localhost
DB_PORT=5555
DB_USER=unwind
DB_PASSWORD=unwind
DB_NAME=unwind

FRONTEND_URL=http://localhost:5173

ANTHROPIC_API_KEY=

# To run tests, create a test database first:
#   docker exec -it unwind-db-1 psql -U unwind -c "CREATE DATABASE unwind_test;"
# Then: npm test (migrations run automatically via pretest)
